[package]
name = "dnaonecalc-host"
version = "0.1.0"
edition = "2021"

[dependencies]
leptos = { version = "0.8.17", default-features = false, features = ["ssr"] }
web-sys = { version = "0.3", features = ["HtmlTextAreaElement", "InputEvent", "KeyboardEvent"] }
wasm-bindgen = "0.2"
