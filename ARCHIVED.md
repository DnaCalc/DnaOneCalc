# ARCHIVED

This repository is archived. Its live tree (as of `c113088`) was merged into
**DnaTreeCalc** at import merge commit `c68ecaa` on 2026-07-12 under program
decision **D5** (`DnaTreeCalc/docs/ux/redesign/REDESIGN_PROGRAM.md`, "Repo
consolidation & crate tiers").

- The live crates continue there as `dnacalc-bench-core`, `dnacalc-bench-host`,
  and `dnacalc-bench-desktop` (renamed from `dnaonecalc-*` in commit `d7ba7ec`).
- Docs, scripts, and fixtures continue under `DnaTreeCalc/docs/onecalc/`,
  `DnaTreeCalc/scripts/onecalc/`, and `DnaTreeCalc/fixtures/onecalc/`.
- `src_archive_ref/` remains here only (pre-rework reference source; it was
  deliberately not imported).

Do not develop here. File changes against DnaTreeCalc.
